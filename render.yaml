services:
  - type: web
    name: lionbay-ai
    env: node
    plan: free
    buildCommand: ./build.sh
    startCommand: cd marketCU && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000 